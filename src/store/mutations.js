export const set_sesionActiva = async (state, elements) => state.sesionActiva = elements

export const set_token = async (state, elements) => state.token = elements

export const set_username = async (state, elements) => state.username = elements
